{
  "name": "@alphomi/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared protocol references and JSON schemas for Alphomi",
  "type": "module",
  "scripts": {
    "typecheck": "node scripts/validate-schemas.mjs",
    "test": "pnpm run typecheck",
    "lint": "pnpm run typecheck"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
