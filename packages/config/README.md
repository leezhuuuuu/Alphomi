# Alphomi Config

This package owns configuration defaults and human-readable config documentation.

## Files

- `defaults/config.example.yaml`: contributor-facing configuration template
- `schema/config-sections.md`: configuration section reference

The repository root keeps a copied `config.example.yaml` for convenience, but the source of truth lives in this package.

For contributor docs, see `docs/guides/configuration.md`.
